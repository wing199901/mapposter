/** OpenFreeMap vector tiles (OpenMapTiles schema). Use TileJSON — direct `{z}/{x}/{y}` URLs return empty tiles. */
export const OPENFREEMAP_TILEJSON = "https://tiles.openfreemap.org/planet"

export const OPENFREEMAP_GLYPHS = "https://tiles.openfreemap.org/fonts/{fontstack}/{range}.pbf"

/** Map band height ratio — matches legacy poster layout. */
export const MAP_BAND_HEIGHT_RATIO = 0.78

export const EXPORT_ATTRIBUTION = "© OpenStreetMap contributors · OpenMapTiles"
